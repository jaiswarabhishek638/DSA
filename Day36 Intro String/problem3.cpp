// getline in string for the paragraph ;

#include<iostream>
using namespace std;
int main(){
    //for any big sentece or paragraph '
    // we use getline(cin,a);

    // input rohit is a good  boy;
    string m;
    cout<<"enter the paragraph: ";
    getline(cin,m);
    cout<<m;
}